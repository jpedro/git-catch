# Git::Catch

Installs git hooks to catch errors before they get committed and released.


## Installation

Add this line to your application's Gemfile:

```ruby
gem 'git-catch'
```

And then execute:

    $ bundle

Or install it yourself as:

    $ gem install git-catch
